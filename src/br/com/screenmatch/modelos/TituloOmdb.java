package br.com.screenmatch.modelos;


// Class Record cria um objeto (imutavel)apenas para representar dados, sem nenhum tipo de comportamento
public record TituloOmdb(String title, String year, String runtime) {
}
